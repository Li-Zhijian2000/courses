#define local
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

int main() {
#ifdef local
  freopen(".txt", "r", stdin);
#endif
  ios::sync_with_stdio(0);
  return 0;
}
